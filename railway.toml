[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir backend app.main:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

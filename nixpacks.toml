[phases.setup]
aptPkgs = ["libgl1", "libglib2.0-0", "libsm6", "libxext6", "libxrender1", "libgomp1"]

[start]
cmd = "gunicorn --chdir backend app.main:app --bind 0.0.0.0:$PORT --workers 4 --timeout 120"

# Nixpacks configuration for Railway deployment

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python -m app.main"

[variables]
PYTHON_VERSION = "3.11"
IMAGE_GEN_MODE = "api"
